{
  "name": "linkedin-ai-post-automation",
  "version": "1.0.0",
  "description": "Scheduled LinkedIn posts generated by Gemini AI with self-review, dry-run mode, and a fail-safe publishing pipeline.",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node scripts/post-automation.js scheduled",
    "dry-run": "node scripts/post-automation.js dry-run",
    "test": "node --test tests/",
    "publish": "node scripts/post-automation.js test-publish",
    "auth": "node scripts/linkedin-auth.js"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "dotenv": "^16.4.0"
  }
}
